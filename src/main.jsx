import React from "react";
import ReactDOM from "react-dom/client";
import NewralApp from "./NewralApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NewralApp />
  </React.StrictMode>
);
